# Agent Rules

- Always use `uv` to manage dependencies and virtual environments, run commands, and execute scripts.

- After any command that generates or reruns a GIF, always show that GIF directly in the Codex reply using a rendered image link (absolute file path).
