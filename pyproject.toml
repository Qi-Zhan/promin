[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "flowviz"
version = "0.2.0"
description = "Program-driven animation from runtime semantics"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "manim>=0.18",
    "pytest>=9.0.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/flowviz"]
